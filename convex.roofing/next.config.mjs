/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
	  remotePatterns: [
		{
		  hostname: "outstanding-bloodhound-351.convex.cloud",
		},
	  ],
	},
  };
  
  export default nextConfig;